#!groovy

node {
    // -------------------------------------------------------------------------
    stage 'Checkout'

    checkout scm

    stage 'Unit Tests'

   
    stage 'Integration'

    stage 'Dist'

    stage 'Package'

    stage 'Publish'
}
