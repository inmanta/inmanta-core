---
description: Fix bug where the `id.attribute_value` field of resources emitted by the exporter have a non-string type, when the type in the model is not a string.
change-type: patch
destination-branches: [master, iso6]
sections:
  bugfix: "{{description}}"
