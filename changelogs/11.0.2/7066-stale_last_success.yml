description: Prevent double processing of events in case of stale events
issue-nr: 7066
change-type: patch
destination-branches: [master, iso7, iso6]
sections:
  feature: "{{description}}"
  known-issue: "Handlers that process events should always indicate when they processed events(via ctx.set_updated)"
