description: Raise a warning if JIT is enabled on the PostgreSQL database as this might result in poor query performance.
change-type: patch
destination-branches: [master, iso7, iso6]
sections:
  minor-improvement: "{{description}}"
