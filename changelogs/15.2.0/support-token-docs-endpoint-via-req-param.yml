---
description: "Added support to provide the access token of the `GET /api/v2/docs` endpoint via the `token` query parameter instead of via the Authorization header."
change-type: patch
destination-branches: [iso8]
sections:
  minor-improvement: "{{description}}"
