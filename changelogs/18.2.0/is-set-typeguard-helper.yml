description: Replace the repeated `x is not None and x is not strawberry.UNSET` checks in the GraphQL schema with an `is_set` TypeGuard helper.
change-type: patch
destination-branches: [master, iso9]
