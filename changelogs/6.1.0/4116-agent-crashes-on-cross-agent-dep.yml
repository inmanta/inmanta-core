---
description: Fix bug that crashes the agent when a cross-agent dependency doesn't have any changes
issue-nr: 4116
change-type: patch
destination-branches: [master, iso5, iso4]
sections:
  bugfix: "{{description}}"
