---
description: Fix bug where the setup.cfg file, generated by the v1tov2 command, contains a dependency to the module itself when the module contains an import for a namespace in its own module.
issue-nr: 4373
issue-repo: inmanta-core
change-type: patch
destination-branches: [master, iso5]
sections:
  bugfix: "{{description}}"
