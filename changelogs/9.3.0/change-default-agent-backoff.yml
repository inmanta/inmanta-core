---
description: The default agent backoff time has been changed from five to three. This backoff is configurable using the `config.agent-get-resource-backoff` config option.
change-type: major
destination-branches: [master]
sections:
  deprecation-note: "{{description}}"
