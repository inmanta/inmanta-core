description: >
    Add ``soft_delete`` option to allow compiles to silently prevent resource
    set deletion if the model is exporting resources included in this set.
issue-nr: 1638
issue-repo: inmanta-lsm
change-type: minor
destination-branches: [master, iso7]
minor-improvement: "{{description}}"
