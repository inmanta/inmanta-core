description: Wrap any exception that occurs during resource export so that it is more useful to the end user
change-type: patch
destination-branches: [master, iso4, iso5]
sections:
    bugfix: "{{description}}"
