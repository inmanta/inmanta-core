description: Fix rare deadlock in the database locking mechanism when tasks are cancelled, mostly affects test environments.
issue-nr: 4384
change-type: patch
destination-branches: [master, iso5, iso4]
sections:
    bugfix: "{{description}}"
