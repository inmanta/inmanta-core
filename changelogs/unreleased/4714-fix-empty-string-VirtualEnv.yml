---
description: make sure that env.VirtualEnv doesn't accepts the empty string as venv path and that use_virtual_env() can't be run if the venv path is an empty string.
issue-nr: 4714
change-type: patch
destination-branches: [master, iso5]

