---
description: Fix issue with "inmanta module build" command if inmanta_plugins dir already exists
issue-nr: 4954
change-type: patch
destination-branches: [master, iso5]
sections:
  bugfix: "{{description}}"
