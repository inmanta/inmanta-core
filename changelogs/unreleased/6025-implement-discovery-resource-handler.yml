description: "Add handler (DiscoveryHandler) to discover unmanaged resources."
issue-nr: 6025
change-type: minor
destination-branches: [master]
sections:
  feature: "{{description}}"
  upgrade-note: "The execution of a handler's do_reload() and execute() now execute between `pre()` and `post()` have been called."
