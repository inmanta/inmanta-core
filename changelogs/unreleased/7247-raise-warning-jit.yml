description: Raise a warning if JIT is enabled on the psql db as this will degrade performances.
change-type: patch
destination-branches: [master, iso7, iso6]
sections:
  minor-improvement: "{{description}}"
