description: >
  Make sure the agent is aware of all possible resource types for a given version so that we don't spawn
  a new venv/executor each time we deploy a subset of these types.
issue-nr: 7722
change-type: patch
destination-branches: [master, iso7]
