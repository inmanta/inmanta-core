description: "Fixed pagination when on a page returning 0 results (due to pagination criteria) that is strictly after or strictly before the bulk of the actual results"
issue-nr: 7898
change-type: patch
sections:
  bugfix: "{{description}}"
destination-branches: [master, iso7]
