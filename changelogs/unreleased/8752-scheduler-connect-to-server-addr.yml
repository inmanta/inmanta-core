---
description: "The agent and the compiler now connect back to the server via the first address in server.bind-address. If it's set to 0.0.0.0, then 127.0.0.1 will be used instead."
issue-nr: 8752
issue-repo: inmanta-core
change-type: patch
destination-branches: [master, iso8]
sections:
  bugfix: "{{description}}"
