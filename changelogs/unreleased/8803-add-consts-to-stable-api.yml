change-type: patch
description: >
  Adds the following consts to the stable api:
    - UNDEPLOYABLE_STATES
    - TRANSIENT_STATES
    - NOT_DONE_STATES
    - DONE_STATES

destination-branches: [master]
sections:
  other-note: "{{description}}"
