description: >
  Python package constraints set in the requirements.txt file at the project level are now
  enforced in the virtual environments of the executors.
issue-nr: 9407
change-type: minor
destination-branches: [master, iso8]
sections:
  feature: "{{description}}"
