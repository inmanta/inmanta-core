---
description: >
  Add a query for resources to the GraphQL endpoint
  Add the capability to sort by multiple fields at the same time
issue-nr: 9531
issue-repo: inmanta-core
change-type: minor
destination-branches: [master]
sections:
  feature: "{{description}}"
  deprecation-note: "The order_by used by the GraphQL endpoint now expects a list instead of a single attribute to filter on"
