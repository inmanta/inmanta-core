---
description: Ensure that the `server_config` and `server_multi` fixture take into account the PostgreSQL host set via the `--postgresql-host` pytest option.
change-type: patch
destination-branches: [master, iso4]
sections:
  bugfix: "{{description}}"
