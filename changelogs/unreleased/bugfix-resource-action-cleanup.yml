---
description: Fix bug where the cleanup job, that removes old resource actions, ignores the environment scope of the `resource_action_logs_retention` setting. This way the most restrictive value of the `resource_action_logs_retention` setting across all environments was applied on all environments.
change-type: patch
destination-branches: [master, iso6]
sections:
  bugfix: "{{description}}"
