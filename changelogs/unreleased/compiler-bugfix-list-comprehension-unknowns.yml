description: "Compiler: fixed bug where a list comprehension over an Unknown (``[x for x in <unknown>]``) would break the compiler under specific circumstances."
change-type: patch
sections:
  bugfix: "{{description}} List comprehensions now return a list with an unknown in it instead of a flat unknown, when they iterate over an unknown."
destination-branches:
  - master
  - iso8
