description: >
  A GraphQL contribution loads the data it adds to the resources query per page with a DataLoader instead of
  contributing SQLAlchemy columns; this removes GraphQLContribution.get_sqlalchemy_columns and
  populate_sqlalchemy_columns and replaces ResourceFilterABC.handles_version with pin_version.
change-type: major
destination-branches: [master, iso9]
