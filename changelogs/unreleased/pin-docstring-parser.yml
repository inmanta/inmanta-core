description: Add upperbound to docstring-parser dependency
change-type: patch
destination-branches: [master, iso3, iso4]
sections:
  bugfix: "Add upperbound to docstring-parser dependency so that pip install does not fail"
