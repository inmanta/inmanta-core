description: Remove deprecated AttributeNotFound exception. Use python's built-in AttributeError instead.
change-type: major
destination-branches: [master]
sections:
  deprecation-note: "{{description}}"

