.. -*- mode: rst -*-

Inmanta core and extensions versions
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

{% if data['component_requirements'] is not none %}
{{ make_list_table(
    ["Component", "Required version"],
    data['component_requirements'],
    title='Component requirements'
    ) }}

.. note::

A machine-consumable json file with these versions is available at https://docs.inmanta.com/inmanta-service-orchestrator-dev/|version_major|/requirements.components.txt

{% else %}
    Components are not pinned for major version |version_major| because it is not released yet.
{% endif %}

