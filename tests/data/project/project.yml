name: test
description: A sample project, used for compiler server tests
modulepath: libs
downloadpath: libs
install_mode: "release"
repo:
    - https://github.com/inmanta/
pip:
    use-system-config: true
environment_settings:
    environment_metrics_retention: 100
    notification_retention: 200

